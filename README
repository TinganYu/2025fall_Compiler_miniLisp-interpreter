# Complier Final Project - miniLisp
---用 lex 跟 yacc 的簡單直譯器---

## 使用環境
* OS: Linux Ubuntu 64-bit
* Tools: Flex, Bison (Yacc), g++

## 功能

功能
1. 可宣告變數 define func/var
2. 可以輸出 print-num, print-bool
3. 四則運算 (numerical & logical)
4. 可以支援條件 if-then-else
5. 可以呼叫 anonymous function
6. 可以定義、呼叫 named fuction
7. 可以支援 recursive function call
8. 會輸出 syntax error

## Lex
### 狀態state：
1. INITIAL： 主要根據 bison 的 token 規則回傳值，例如：ID、NUM、+-*/等
2. PARAM： 主要用來蒐集 function 的 parameters 直到括號對齊
3. BODY： 主要用於蒐集 IF 以及 Funcion 的內容物，並回傳 BODY_STR (用於之後recursive parser)

### Buffer Control：
1. `push_lexer_buffer`：儲存目前 lex 資料(buffer)，並匯入新的 string buffer 給 lex
2. `pop_lexer_buffer`：將目前的 lex buffer 刪除，恢復成舊的

## Yacc

### Symbol Table：
1. func_table：存所有函數
2. env_stack：模擬 Function Call Stack 存變數(scope)

### Buffer Control：
先 push 新的 env 接著執行 `recursive_parse_eval`，結束後再 pop

`recursive_parse_eval`：
使用 **push_lexer_buffer** 與 **pop_lexer_buffer** 控制 Lexer 的輸入流，實現字串輸入的動態分析。
同時開啟新的 parser()。

### 程式邏輯：
前面的print以及四則運算基本上沒什麼特別的，直接執行。

Define 會先檢查有沒有重複定義。比較特別是，Define 會需要先存入ID (Placeholder)，這樣後面 function 在檢查的時候，如果裡面有 recursive 才不會 'function not defined'。

主要比較複雜的部份是 If 以及 Function，因為我要避免  Expression 馬上執行結果，所以先用 `BODY_STR` 蒐集它的內容物，等到實際要使用的時候 (例如：function call, 或是 if test-case 結果知道後)，再呼叫  `recursive_parse_eval` 進入新的 parser，讓 lex 重讀 body 內的程式。(IF 這樣做的目的一樣是為了 recursive function 不要無限迴圈)

Function Call 則是會先檢查 function exsisted & argument number，接著才會 bind parameter with argument value，接著使用 `recursive_parse_eval` 實際執行結果。

我額外增加 syntax_only_mode ，是因為我想要在 define 的時候，先檢查 funcEXP 的 body 內容是正確的 (避免後來 func call 沒被呼叫，就沒被發現)。而以防任何輸出，我設定在這個情況下只要檢查就好。

任何 recursive_parse_eval 中檢查出錯誤，都會將 error 設為 1，馬上 YYABORT 結束程式。


此外，透過 `function.h` 定義了跨模組共享的資料結構：
- Function 結構：
  - vector<string> parameters: 紀錄參數清單。
  - string body: 以字串形式保存函式主體，用來之後達成延遲求值。
## 如何執行
```bash
bison -d -o y.tab.c final_1.y
g++ -c -g -I.. y.tab.c
flex -o lex.yy.c final_1.l
g++ -c -g -I.. lex.yy.c
g++ -o final y.tab.o lex.yy.o -ll
./final < input.lsp
```